# 2026-05-20 进展记录

- 完成对局完整性第一轮优化：新增系统提醒、本人技能说明、公开技能展示和牌桌式 UI。
- 共享视图扩展：`PublicGameView.systemHints`、`PublicPlayerView.characterSkills`、`PrivatePlayerView.ownSkills`；新增 `SkillView` 和 `SystemHintView`。
- 服务端新增 `server/src/engine/skill-registry.ts`，集中维护常规技能与首批 10 个角色技能的 MVP 说明文案。
- `server/src/view/public-view.ts` 现在按玩家视角生成系统提醒，覆盖宣胜、技能、传递、响应、接收/拒收、濒死、游戏结束等阶段。
- 修复隐藏角色信息泄露：他人视角下隐藏且未揭示角色不再下发 `characterName`、`characterImageUrl`、`characterSkills`；本人仍能看到自己的隐藏角色和技能。
- 前端 `GameBoard` 增加系统提醒卡片和“我的技能”技能书，展示常规技能剩余次数和角色技能说明。
- 前端 `App` 改为左房间信息、中牌桌、右日志的布局；`PlayerList` 改为环形牌桌座位卡，支持当前行动玩家、本人、死亡、濒死高亮。
- 样式更新为更接近页游牌桌的暗色桌面风格，并增加窄屏响应式降级、日志滚动区、连接状态样式。
- 补充测试：隐藏角色脱敏、本人技能可见、公开角色技能可见、系统提醒不泄露隐藏角色、技能注册表覆盖所有 MVP 角色技能。
- 验证结果：`npm run typecheck` 通过，`npm test` 通过（3 个测试文件、19 个测试），`npm run build` 通过。
- 部署排障记录：用户在 Ubuntu 服务器访问 IP 看到 “Welcome to nginx!” 默认页，判断前端构建产物未被当前 Nginx server block 命中；常见原因是默认站点仍启用、项目 Nginx 配置未启用或 `server_name` 不匹配、`root` 未指向 `/opt/wujianfengyun/client/dist`、未执行 `npm run build` 或未 reload Nginx。
- 部署排障记录：用户执行 Nginx 配置后出现 `unknown directive "proxySETHEADEER"`，判断为 Nginx 配置文件首行/指令拼写错误，同时命令中也出现 `ln` 写成 `In`、`systemctl` 写成 `systemcti`、项目目录 `wujianfengyun`/`wujiarnfengyun` 拼写不一致等问题；建议直接重写 `/etc/nginx/sites-available/wujianfengyun` 为标准配置后 `nginx -t` 与 `systemctl reload nginx`。
- 部署排障记录：用户的 `wujianfengyun-server.service` 反复 auto-restart，`ExecStart` 指向 `/opt/wujianfeingyun/server/dist/index.js`，而当前目录是 `/opt/wujianfengyun`，判断为 systemd 服务文件项目路径拼写错误；同时用户把 `ss -lntp` 输成 `ss -intp`，应先重写 systemd service 的 WorkingDirectory/ExecStart，再 `daemon-reload`、`npm ci && npm run build`、重启服务并看 `journalctl -u wujianfengyun-server -n 100 -l --no-pager`。
- 部署排障记录：用户修正 ExecStart 到 `/opt/wujianfengyun/server/dist/index.js` 后，服务仍然 `status=1/FAILURE` 并 auto-restart；下一步需要查看 `journalctl -u wujianfengyun-server -n 100 --no-pager -l`、确认 `server/dist/index.js` 存在，并在项目目录直接运行 `PORT=8787 NODE_ENV=production node server/dist/index.js` 暴露真实 Node 报错。
- 部署故障根因确认：日志显示 `TypeError [ERR_UNKNOWN_FILE_EXTENSION]: Unknown file extension ".ts" for /opt/wujiannfengyun/shared/src/index.ts`，原因是 `shared/package.json` 的 exports 在生产环境仍指向 `./src/index.ts`。
- 已修复 monorepo 生产导出：将 `@wujian/shared` exports 改为 `./dist/index.js` 与 `./dist/index.d.ts`；本地 `npm run typecheck`、`npm test`、`npm run build` 均通过。服务器下一步需拉取最新代码，重新安装/构建并重启 systemd。
- 方案 B 已执行：已将生产导出修复连同系统提醒、技能说明、牌桌 UI 优化提交并推送到 GitHub `main`，提交 `4474bb9 Improve gameplay guidance and production shared exports`。服务器可通过 `git pull && npm ci && npm run build && systemctl restart` 应用修复。
- 部署优化需求：用户反馈当前部署太麻烦，确认选择“一键脚本”并要求“预留 HTTPS”。已设计并实现脚本化部署方案，避免用户手动输入 systemd/Nginx 配置导致拼写错误。
- 新增部署脚本：`deploy/install.sh`（首次部署）、`deploy/update.sh`（更新部署）、`deploy/status.sh`（只读诊断）。脚本支持 `--domain`、`--project-dir`、`--branch`、`--port`、`--https reserved|enabled|off`、`--skip-tests` 等参数。
- 部署策略更新：生产环境统一通过 Nginx `/ws` 代理 WebSocket，`client/.env.production` 写入 `VITE_WS_URL`；IP 临时测试使用 `ws://IP/ws`，域名部署默认预留 `wss://域名/ws`，HTTPS 证书由 Certbot 后续启用或通过 `--https enabled` 尝试自动申请。
- 文档更新：根 `README.md` 的 Ubuntu 部署章节改为优先推荐一键部署/更新/诊断，并新增 `deploy/README.md` 说明脚本参数、HTTPS、常见问题和排障命令。
- 验证结果更新：`bash -n deploy/install.sh && bash -n deploy/update.sh && bash -n deploy/status.sh` 通过，`npm run typecheck` 通过，`npm test` 通过（3 个测试文件、19 个测试），`npm run build` 通过。
